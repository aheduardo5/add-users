import React from "react";

const Header = ({updateH}) => {
  console.log(updateH)
  return (
    <div>
      <h1>
        user manager()
        <span>
          <button > Agregar</button>
        </span>
      </h1>
    </div>
  );
};

export default Header;
